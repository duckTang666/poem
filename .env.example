# 前端请求基础路径（Vite 中通过 import.meta.env.VITE_API_BASE_URL 读取）
# 开发时使用代理到后端：http://localhost:3000
VITE_API_BASE_URL=/api

# 后端数据库连接配置（server/index.cjs 会读取）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=poem_db

# 后端服务端口（可选，默认 3000）
PORT=3000